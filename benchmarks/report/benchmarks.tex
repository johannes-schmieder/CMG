\documentclass[10pt]{article}
\usepackage[margin=0.80in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{array}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{siunitx}
\usepackage{seqsplit}
\usepackage{tabularx}

\providecommand{\BenchmarkFigureRoot}{figures}
\providecommand{\BenchmarkDataRoot}{data}
\input{\BenchmarkDataRoot/current_results.tex}

\definecolor{RustBlue}{HTML}{0072B2}
\definecolor{MatlabOrange}{HTML}{D55E00}
\hypersetup{
  pdftitle={Current Production CMG Benchmark Qualification},
  pdfauthor={CMG maintainers},
  colorlinks=true,
  linkcolor=RustBlue!75!black,
  urlcolor=RustBlue!75!black
}
\setlist{nosep,leftmargin=*}
\captionsetup{font=small,labelfont=bf}
\pagestyle{fancy}
\fancyhf{}
\lhead{CMG current Rust/MATLAB qualification}
\rhead{\thepage}
\setlength{\headheight}{13.6pt}
\setlength{\tabcolsep}{5pt}
\sisetup{detect-all}

\title{Current Production CMG Benchmark Qualification\\
\large Rust versus the official MATLAB/C MEX package}
\author{CMG maintainers}
\date{Updated 29 August 2026}

\begin{document}
\maketitle

\begin{abstract}
This report describes the current production comparison between the Rust package and the official MATLAB solver with its default C MEX build.  Five deterministic connected graphs, each with one million vertices and between about one and eight million canonical edges, were evaluated at 1, 8, 16, and 32 application CPUs.  Each reported point is the median of \ReportRepetitions{} measurements after \ReportWarmups{} warmups.  At 16 CPUs, geometric-mean Rust/MATLAB ratios are \ReportSixteenSetupRatio{} for hierarchy setup, \ReportSixteenApplyRatio{} for stationary preconditioner application, \ReportSixteenPcgRatio{} for reused-preconditioner PCG, \ReportSixteenTotalRatio{} for setup plus one solve, and \ReportSixteenRssRatio{} for process peak resident memory.  Rust is faster in setup-plus-solve for every family at every tested CPU count.  Sixteen CPUs minimize Rust's aggregate single-right-hand-side latency on the tested hardware; 32 CPUs remain supported but regress for every family.
\end{abstract}

\section{Decision summary}

\noindent\fcolorbox{RustBlue!45}{RustBlue!4}{%
\parbox{0.955\linewidth}{%
\textbf{Measured default on Intel Xeon Gold 6242: 16 application CPUs for one large right-hand side.}
At that setting, Rust takes \ReportSixteenRustTotal{} seconds in geometric mean versus \ReportSixteenMatlabTotal{} seconds for MATLAB, a Rust/MATLAB ratio of \ReportSixteenTotalRatio{}.  Rust's one-to-16-CPU speedup is \ReportRustSpeedupSixteen{}x, versus \ReportMatlabSpeedupSixteen{}x for MATLAB.  Moving from 16 to 32 CPUs multiplies Rust total time by \ReportRustThirtyTwoPenalty{} and MATLAB total time by \ReportMatlabThirtyTwoPenalty{}.  Grid and weak-community inputs are individually fastest in Rust at 8 CPUs; the other three families are fastest at 16.}}

The principal comparison is the complete native package workflow, not an isolated kernel.  Rust's setup-plus-solve total includes hierarchy construction, parallel-plan construction when routed, and PCG.  MATLAB's total includes its official \texttt{cmg\_precondition} workflow followed by \texttt{pcg}.  The matrix is already resident, so binary input and graph or sparse-matrix assembly are excluded.  Lower Rust/MATLAB ratios favor Rust throughout this report.

\section{Benchmark configuration and protocol}

The benchmark is accepted run \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportRunId}}, SGE array \ReportJobId.  It evaluates Rust source \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportSourceCommit}} against the official CMG source pinned at \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportUpstreamCommit}}.  The figures are generated from the run's 40 reduced configuration rows and checked numerically against the maintained compact record before the PDF is compiled.

\clearpage
\begin{table}[!htbp]
\centering
\caption{Controlled study design.}
\label{tab:design}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.27\linewidth}>{\raggedright\arraybackslash}X}
\toprule
Item & Current qualification \\
\midrule
Implementations & Current Rust production package; official MATLAB package with its default C MEX build \\
Toolchains & Rust \ReportRustVersion{}; MATLAB \ReportMatlabVersion{} \\
Hardware & \ReportCpuModel{}; whole-node 32-slot SGE allocation; application limits of 1, 8, 16, and 32 CPUs \\
Graphs & Weighted path; 2D grid; worker-firm degree 3; worker-firm degree 16; weak community; all one million vertices \\
Timing & \ReportWarmups{} warmups followed by \ReportRepetitions{} measured repetitions; medians reported; per-configuration bootstrap intervals shown for total time \\
Accuracy & Native stopping logic in each package; tolerance \ReportTolerance{}; at most 1,000 iterations; independent residual and backward-error checks retained \\
Identity & Shared deterministic graph, right-hand side, truth vector, and metadata bytes; hashes recorded per task \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Measured stages}

\begin{description}[style=nextline]
\item[Hierarchy/preconditioner setup] CMG hierarchy and terminal factor construction.  Rust reports reusable parallel-plan construction separately, and the plan is included in total time.
\item[Stationary preconditioner apply] One normalized CMG application with an existing hierarchy and workspace.  Fast applications are looped and divided by the loop count.
\item[Reused-preconditioner PCG] One PCG solve after setup.  The implementations retain their native stopping rules, so timings do not hold iteration counts or residual definitions fixed.
\item[Setup + plan + solve] Fresh hierarchy setup, Rust plan setup when applicable, and one solve.  This is the main user-facing latency measure.
\item[Peak RSS] Process peak resident set size from \texttt{/usr/bin/time -v}; this includes input and runtime storage and is not an allocation-by-allocation decomposition.
\end{description}

\clearpage
\section{Rust and MATLAB on the same timing axes}

Figure~\ref{fig:stage-times} puts the Rust and MATLAB lines together for every primary stage.  It is the quickest absolute comparison: Rust setup is substantially lower at every CPU count; PCG is close to MATLAB at one CPU and separates at 8 and 16 CPUs; both implementations show limited end-to-end scaling because hierarchy work and overhead remain important.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{\BenchmarkFigureRoot/current_stage_timings.pdf}
\caption{Geometric-mean stage times across the five graph families.  Rust is the solid blue line with circles; MATLAB is the dashed orange line with squares.  Both implementations share a zero-based linear time axis within each panel.}
\label{fig:stage-times}
\end{figure}

\clearpage
\section{Stage ratios}

Figure~\ref{fig:stage-ratios} shows the corresponding Rust/MATLAB ratios for each family and the geometric mean.  The ratio view makes heterogeneity visible without hiding the absolute scale.  At 16 CPUs, the geometric-mean ratios are \ReportSixteenSetupRatio{}, \ReportSixteenApplyRatio{}, \ReportSixteenPcgRatio{}, and \ReportSixteenTotalRatio{} for setup, apply, PCG, and total time.  One one-CPU stationary-apply point is slightly above one; the main total is below one for all 20 family/CPU combinations.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.84\textwidth]{\BenchmarkFigureRoot/current_stage_ratios.pdf}

\vspace{0.3em}
{\footnotesize\textbf{Aggregate geometric means (Rust / MATLAB)}\par}
\vspace{0.15em}
{\footnotesize\ReportAggregateTable}
\caption{Family-level Rust/MATLAB timing ratios plus the geometric mean.  The dotted reference is parity; values below one favor Rust.  The table gives the exact geometric-mean ratios at each CPU count.}
\label{fig:stage-ratios}
\end{figure}

\clearpage
\section{Family-level setup-plus-solve comparisons}

The complete total-time curves in Figure~\ref{fig:family-times} place MATLAB directly beside Rust for each graph family.  Rust's best measured setting is 16 CPUs for the weighted path and both worker-firm graphs, and 8 CPUs for the grid and weak-community graphs.  MATLAB's best setting is 8 CPUs for dense worker-firm and 16 CPUs for the remaining families.  The error bars summarize resampling uncertainty within each implementation; they do not convert this controlled qualification into a population-level hardware claim.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{\BenchmarkFigureRoot/current_family_totals.pdf}
\caption{Setup-plus-plan-plus-solve latency by family.  Rust and MATLAB share every panel and axis.  Points are medians of seven measurements; bars are the reducer's per-implementation bootstrap intervals.}
\label{fig:family-times}
\end{figure}

\clearpage
Figure~\ref{fig:family-ratios} gives the direct ratio view.  At 16 CPUs, Rust total time ranges from 0.248 times MATLAB on dense worker-firm to 0.656 times MATLAB on the path.  The 16-CPU family results in Table~\ref{tab:family-sixteen} show the absolute times and the different native iteration counts together.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{\BenchmarkFigureRoot/current_family_total_ratios.pdf}
\caption{Rust/MATLAB setup-plus-solve ratio for every family and CPU count.  Values below one favor Rust.}
\label{fig:family-ratios}
\end{figure}

\begin{table}[htbp]
\centering
\caption{One-million-vertex results at 16 application CPUs.}
\label{tab:family-sixteen}
\resizebox{\textwidth}{!}{\ReportFamilyTable}

\vspace{0.35em}
\footnotesize Times are medians.  ``R/M'' is Rust total divided by MATLAB total.  Iterations follow each package's native stopping rule.
\end{table}

\section{CPU scaling interpretation}

The aggregate Rust total falls from 2.498 seconds at one CPU to \ReportSixteenRustTotal{} seconds at 16 CPUs, then rises to 1.803 seconds at 32 CPUs.  Every Rust family regresses from 16 to 32 CPUs: the increase ranges from about 10 percent on dense worker-firm to 55 percent on the grid.  The likely mechanism is the combined cost of scheduling, synchronization, fixed-order reductions, and memory traffic once additional workers no longer receive enough useful work.  This is an inference from the uniform 16-to-32 pattern, not a direct causal decomposition.

For single-right-hand-side latency on Gold 6242, 16 CPUs are therefore the measured aggregate default, with 8 CPUs worth testing on sparse spatial or community graphs.  Thirty-two CPUs remain a supported configuration but are not the preferred setting for this workload.

\clearpage
\section{Memory comparison}

Figure~\ref{fig:memory} shows Rust and MATLAB together in absolute units and presents the family-level ratios.  At 16 CPUs, Rust's geometric-mean peak RSS is \ReportSixteenRssRatio{} times MATLAB's.  Family ratios range from 0.121 on the path to 0.533 on dense worker-firm.  The dense case still uses 3.52 GiB in Rust, so the ratio should not be mistaken for a claim that large dense inputs are memory-free.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{\BenchmarkFigureRoot/current_memory.pdf}
\caption{Process peak RSS.  Left: geometric-mean absolute memory with Rust and MATLAB on the same axis.  Right: family-level Rust/MATLAB ratios and their geometric mean.}
\label{fig:memory}
\end{figure}

\section{Numerical validity and iteration counts}

All \ReportConfigurations{} configurations converged and passed the scheduler, identity, timing, and numerical validators.  Rust's maximum backward error is \ReportMaxRustBackward{}.  Its maximum independently recomputed relative residual is \ReportMaxRustResidual{}, while MATLAB's maximum native relative residual is \ReportMaxMatlabResidual{}.  The larger independent Rust residual is compatible with the package's retained stopping and certification conventions; the report does not claim identical residual definitions.

Rust uses fewer iterations than MATLAB for every family at 16 CPUs (Figure~\ref{fig:iterations}), ranging from 9 versus 11 on dense worker-firm to 53 versus 62 on the path.  Runtime differences therefore combine implementation speed with the packages' certified native iteration paths.  They should not be interpreted as fixed-work microbenchmarks.

\clearpage
\begin{figure}[htbp]
\centering
\includegraphics[width=0.91\textwidth]{\BenchmarkFigureRoot/current_iterations.pdf}
\caption{PCG iterations at 16 CPUs under each package's native stopping rule.  Iteration counts are invariant across the tested CPU grid within each implementation and family.}
\label{fig:iterations}
\end{figure}

MATLAB reports official hierarchy flag 3 for the four dense worker-firm configurations.  All four outputs converge and pass the numerical validator.  The warnings remain explicit and are not silently discarded or converted into retries.

\section{Provenance and acceptance}

The accepted array contains five tasks and 40 implementation/CPU configurations.  SGE reports \texttt{failed=0} and \texttt{exit\_status=0} for every task; task wall times range from 1,101 to 2,822 seconds and recorded \texttt{maxvmem} ranges from 9.05 to 13.47 GiB.  Each task also has an application-success receipt, a validation receipt, input hashes, topology, and process-memory records.

\noindent\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.25\linewidth}X}
\toprule
Identity & Accepted value \\
\midrule
Protocol & \texttt{\ReportProtocol} \\
Run & \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportRunId}} \\
Rust source & \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportSourceCommit}} \\
Source archive SHA-256 & \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportSourceArchive}} \\
Rust benchmark binary SHA-256 & \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportRustBinary}} \\
Official upstream source & \texttt{\footnotesize\expandafter\seqsplit\expandafter{\ReportUpstreamCommit}} \\
Compact accepted record & \path{.ci/performance/scc-rust-matlab-current.json} \\
Detailed retained reduction & \path{benchmarks/report/data/current_results.csv} \\
\bottomrule
\end{tabularx}

\vspace{0.6em}
The generator requires the complete 5-family by 2-implementation by 4-CPU grid and verifies every row's identity.  It recomputes the geometric means and checks them against the compact accepted JSON.  PDF compilation stops if a check differs beyond numerical roundoff.

\end{document}
