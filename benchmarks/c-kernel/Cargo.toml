[package]
name = "cmg-c-kernel-bench"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false
license = "GPL-3.0-only"

[dependencies]
cmg = { path = "../.." }

[build-dependencies]
cc = "1.2"
