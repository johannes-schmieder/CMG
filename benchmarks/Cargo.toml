[package]
name = "cmg-benchmarks"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
cmg = { path = ".." }

[[bin]]
name = "terminal-build"
path = "src/bin/terminal-build.rs"
