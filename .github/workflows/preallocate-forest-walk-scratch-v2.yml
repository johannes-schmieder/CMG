name: Lint-repaired preallocated forest-walk scratch gate
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/preallocate-forest-walk-scratch-v2.yml
permissions:
  contents: write
jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python scripts/preallocate_forest_walk_scratch_gate_v2.py
