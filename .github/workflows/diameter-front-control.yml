name: Direct diameter-front control gate

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diameter-front-control.yml'

permissions:
  contents: write

concurrency:
  group: cmg-diameter-front-control
  cancel-in-progress: false

jobs:
  benchmark-and-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Qualify direct branch-free diameter-front control
        shell: bash
        run: |
          set -euo pipefail
          python scripts/run_diameter_front_control_gate.py

      - name: Show retained decision
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/diameter-front-control-latest.json || true
