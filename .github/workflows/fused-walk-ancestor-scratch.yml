name: Fused walk-ancestor scratch gate

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fused-walk-ancestor-scratch.yml'

permissions:
  contents: write

concurrency:
  group: cmg-fused-walk-ancestor-scratch
  cancel-in-progress: false

jobs:
  benchmark-and-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Qualify fused walk and ancestor-prefix scratch
        shell: bash
        run: |
          set -euo pipefail
          python scripts/fused_walk_ancestor_scratch_gate.py

      - name: Show retained decision
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/fused-walk-ancestor-scratch-latest.json || true
