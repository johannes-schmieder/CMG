name: Monitor dense endpoint-axis confirmation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/monitor-dense-endpoint-axis-confirmation.yml'

permissions:
  actions: read
  contents: write

concurrency:
  group: cmg-dense-endpoint-axis-confirmation-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 65
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Wait for confirmation and preserve result or logs
        shell: bash
        run: |
          set -euo pipefail
          python scripts/monitor_dense_endpoint_axis_confirmation.py

      - name: Show confirmation status
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/dense-endpoint-axis-confirmation-run-status.json || true
          git show origin/main:.ci/performance/dense-endpoint-axis-sort-latest.json || true
