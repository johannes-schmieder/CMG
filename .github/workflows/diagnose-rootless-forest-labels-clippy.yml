name: Diagnose rootless forest-label Clippy failure

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diagnose-rootless-forest-labels-clippy.yml'

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Capture exact Clippy diagnostic and restore source
        shell: bash
        run: |
          set -euo pipefail
          python scripts/diagnose_rootless_forest_labels_clippy.py

      - name: Show diagnostic
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/rootless-forest-labels-clippy.json || true
