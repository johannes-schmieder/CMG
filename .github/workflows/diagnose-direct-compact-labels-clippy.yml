name: Diagnose direct compact-label Clippy failure

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diagnose-direct-compact-labels-clippy.yml'

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Capture exact Clippy diagnostic and restore source
        shell: bash
        run: |
          set -euo pipefail
          python scripts/diagnose_direct_compact_labels_clippy.py

      - name: Show diagnostic
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/direct-compact-labels-clippy.json || true
