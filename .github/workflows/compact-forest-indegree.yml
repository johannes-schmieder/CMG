name: Compact forest-indegree gate

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/compact-forest-indegree.yml'

permissions:
  contents: write

concurrency:
  group: cmg-compact-forest-indegree
  cancel-in-progress: false

jobs:
  benchmark-and-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Qualify compact forest indegrees
        shell: bash
        run: |
          set -euo pipefail
          python scripts/compact_forest_indegree_gate.py

      - name: Show retained decision
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/compact-forest-indegree-latest.json || true
