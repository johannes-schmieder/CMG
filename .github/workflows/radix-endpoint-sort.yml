name: Adaptive radix endpoint-sort gate

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/radix-endpoint-sort.yml'

permissions:
  contents: write

concurrency:
  group: cmg-radix-endpoint-sort
  cancel-in-progress: false

jobs:
  benchmark-and-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Qualify adaptive radix endpoint sorting
        shell: bash
        run: |
          set -euo pipefail
          python scripts/radix_endpoint_sort_gate.py

      - name: Show retained decision
        if: always()
        shell: bash
        run: |
          git fetch origin main
          git show origin/main:.ci/performance/radix-endpoint-sort-latest.json || true
